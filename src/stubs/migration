import { Migration } from '../helpers/database/Migration';

class {{ class }} extends Migration{
    async static up(){
        await queryRunner.query(
            //''
        );
    }

    async static down(){
        await queryRunner.query(
            //''
        );
    }
}